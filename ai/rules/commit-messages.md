# Commit Messages

Follow the style already established in this repo's history: `<type>: <short description>`, lowercase, imperative mood (e.g. `feat: add temperature reported ingestion endpoint`, `fix: correct alert idempotency check`).

Common types: `feat`, `fix`, `docs`, `refactor`, `test`, `chore`.
